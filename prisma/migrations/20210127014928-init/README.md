# Migration `20210127014928-init`

This migration has been generated by Matt Zuzolo at 1/26/2021, 8:49:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "Producer.name_unique" ON "public"."Producer"("name")

CREATE UNIQUE INDEX "Style.name_unique" ON "public"."Style"("name")

CREATE UNIQUE INDEX "Whisky.name_unique" ON "public"."Whisky"("name")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201219195323-init..20210127014928-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,9 +13,9 @@
 model Whisky { // The Balvenie 12, Lagavulin 16, Angel's Envy
   id String @id @default(uuid())
   createdAt  DateTime  @default(now())
-  name String
+  name String @unique
   blended Boolean
   age Int? // Maybe default to zero?
   abv Int? // we might not have this information
@@ -29,9 +29,9 @@
 }
 model Style {
   id String @id @default(uuid())
-  name String // "Single malt scotch whisky", "Kentucky straight bourbon whiskey"
+  name String @unique // "Single malt scotch whisky", "Kentucky straight bourbon whiskey"
   shortName String // "Scotch whisky", "Bourbon whiskey" 
   // Relationship
   regionId  String?
@@ -43,10 +43,9 @@
 model Producer { // The Balvenie, Lagavulin, Buffalo Trace
   id String @id @default(uuid())
   createdAt  DateTime  @default(now())
-
-  name String
+  name String @unique
   // Relationship
   whiskys Whisky[]
```


